# פרומפט להתחלת סשן חדש — לידרפיד

> העתק את כל הבלוק שמתחת לקו והדבק כהודעה הראשונה בסשן החדש.

---

אני אורי. אנחנו עובדים על **לידרפיד (LeaderFeed)** — אפליקציית מודיעין חדשות נדל"ן בעברית.

- **ריפו:** `C:\Users\ori19\Documents\news-generator-deploy` (⚠️ **לא** `klikavault/news-generator` — משם לא נפרס!)
- **חי:** https://news-generator-seven.vercel.app · פרויקט Vercel `news-generator`
- **פריסה:** push ל-master → GitHub Action פורס אוטומטית
- **DB:** Supabase `uhexangjjtgnlnxinuyj`
- **בן סולומון** = המייסד שמקבל את הודעות הסיכום (בווטסאפ). אני אורי, אני זה שמדבר איתך.

## כלל הברזל (הכי חשוב)
**מקסימום ביצועים מול מקסימום חיסכון בטוקנים.** קריאות AI/Claude **רק בלחיצה מפורשת של המשתמש** + cached. כל השאר = SQL/חיפוש חינמי, אפס טוקנים. אף פעם לא AI בסריקות אוטומטיות.

## כללי עבודה
- `git pull` לפני שינוי · לא לשכתב קבצים שלמים · לא לשנות עיצוב בלי שביקשתי
- **QA בפינצטה**: לבדוק ויזואלית (דסקטופ+מובייל) לפני שאומרים "בוצע"
- לא לכתוב "גוגל לעסקים" בשום מקום ציבורי (סיכון תביעה) — "גוגל פנימי" זה מונח פנימי בלבד
- עברית ב-curl נשברת ב-Windows → להשתמש ב-python (urllib + json.dumps)
- הטרמינל (cp1255) נשבר על אימוג'ים/חצים → לכתוב לקובץ ולקרוא עם Read
- קומיטים חתומים `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`

## מצב נוכחי — הכל פרוס, חי ומאומת ✅
HEAD = `af14469`, master מסונכרן, working tree נקי.

### 🔴 16.8 — אודיט "הבית דליל": 6 פידים מתים + 3 כשלים שקטים נוספים
**התלונה הייתה "הבית דליל מטבעו". זה לא היה טבעי — זה היה שבור.**
עמוד הבית: **7 → 36 ידיעות** באותו יום עצמו (אותו חלון בדיוק).

מה שנמצא, כולו **בלי שאף פעם נזרקה שגיאה**:
1. **6 מ-10 הפידים הנדל"ניים מתו.** פיד מת לא נראה ככשל — גלובס החזיר
   `200 OK` עם `<rss version="2.0"/>` (61 בתים), Ynet כסף גוף באורך 0,
   וואלה ערוץ ריק, TheMarker 403 ל-UA שלנו בלבד, כלכליסט וביזפורטל
   כתובות שבוטלו. תוקן: גלובס→iID=607, Ynet→StoryRss6, וואלה→feed/13111,
   TheMarker קיבל UA ייעודי. ynet 1854 היה ממותג "נדל״ן" אבל שמו האמיתי
   "מבזקים" — הוא בלע 287 מ-907 הדירוגים.
2. **הסריקה איבדה פידים בכל ריצה.** 103 פידים במקביל עם timeout 10ש' →
   68 מ-103 "נכשלו" בזמן שסדרתית הם עונים בפחות מ-2ש'. נוסף `mapPool`
   עם תקרה 16.
3. **גרשיים בעברית הרגו חלקי דירוג שלמים.** `"לנדל"ן"` בתוך מחרוזת JSON
   סוגר אותה באמצע → `JSON.parse` נופל → כל 25 הכתבות בחלק נמחקות.
   27 מ-40 הנימוקים מכילים גרשיים, כלומר כמעט כל חלק מת. נוסף
   `repairHebrewQuotes`. אומת: scored 0 → 25.
4. **הקליטה נחנקה** על `57014 statement timeout` (המטען גדל פי 5) והסריקה
   חרגה מתקרת 60ש' של Vercel. תוקן: upsert בחלקים של 300, גבול דירוג
   תלוי-זמן, timeout פידים 10ש'. סריקה בפרודקשן: 60.9ש' כשל → **21.3ש' ✅**

**🛡️ הניטור (העיקר):** `lib/feed-health.ts` + `/api/feed-health` + מחובר
להתרעה היומית של `model-health` (03:40, בלי קרון חמישי). **פיד שמחזיר
אפס פריטים = כשל, בדיוק כמו חריגה** — מייל לבן ברגע שמקור מדורג מת.
בדיקה ידנית: `curl https://news-generator-seven.vercel.app/api/feed-health`

**⚠️ נותרו 21 פידים מקומיים מתים** (ingest-only, פוגעים רק במחקר-ערים,
לא בבית). הם מופיעים ב-`deadIngestOnly` ושורפים ~10ש' timeout כל סריקה.
שווה לנקות — לשאול את אורי קודם.

**מה נבנה בסשן הקודם (הכל חי):**
1. **🌐 מחקר שיוצא לרשת (Firecrawl)** — כשהמאגר דל, המחקר בערים יוצא לרשת. נושאים אזרחיים+מותאמים = תמיד רשת; נדל"ן = רק כשפנימי <4 (חיסכון). cached 24ש'.
2. **📈 תהליך הבשלה** — `/api/cities/maturation`: ציר זמן מתוארך על פני שנים + **מצב מיקוד** (יזם + פרויקט ספציפי) + באנר 🟢 התחלה → 🔴 היום. אומת: רני צים מעלות = קשת 13 שנים.
3. **נושאי מחקר חדשים** — ספורט / תרבות / אירועים + העשרת 14 הקיימים.
4. **🌲 פורטל הצמיחה של הגליל** — `/galil`: הטמעת הפורטל מלאבאבל בעיצוב לידרפיד (סיפורים מתפתחים, "חשוב לדעת", תגי השפעה, מספרים גדולים, נרטיב) + רצועת "טרי מהשטח" חיה.
5. **🔎 חיפוש הדיפפיד יוצא לרשת** — `/api/archive` מצרף תוצאות-רשת כשיש <3 פנימיות, עם הקשר-נדל"ן אוטומטי (`reWebQuery`).

**באגים שתוקנו:** מסנן `classify.ts` שפסל כתבות ב-substring ("ירי" בתוך "מחירים") → הבית הראה 0 · `scanner.ts` שדירג רק שורות חדשות → כתבות-יום נתקעו בלי ציון · webhook של Apify שהצביע לכתובת מוגנת-SSO → פייסבוק תקוע מ-12.6 · מגבלת קצב Firecrawl.

## עלויות
- **Firecrawl** — free tier, 1000 קרדיט/חודש, 2 קרדיט לחיפוש. המפתח חי ב-Vercel prod. $0.
- **Apify** (פייסבוק) — ⚠️ הקרדיט החינמי כמעט אזל ($4.70/$5), מתאפס ב-1 לחודש. הסריקה השבועית אולי תדלג עד אז — זה מסתדר לבד.
- **Claude** — sonnet-4-6 (יצירה) + haiku-4-5-20251001 (דירוג). יש הגנה אוטומטית ממודלים מתים (self-heal + ניטור יומי + מייל).

## קרונים (vercel.json)
`model-health` 03:40 · `scan` 04:00 (=07:00 ישראל) · `fb-scan` ראשון 05:00 · `city-facts` 05:30.
**אורי החליט: סריקה פעם ביום ב-7 בבוקר + שאר ההרצות ידניות. לא לשנות תזמון.**

### 🔬 25.8 — אודיט לפני פרזנטציה: המחקר "עבד" אבל לא חקר את העיר
אורי ביקש לוודא שהמחקר **באמת** מוצא אלימות/פשיעה ושהתאריכים מעודכנים.

**מה נמצא:** מחקר "אלימות" על נהריה/בית שאן/אשקלון החזיר **1 מתוך 18
תוצאות שבכלל הזכירה את העיר**, וכולן בלי תאריך. השאר: מסמכי כנסת על
רצח נשים, ארכיון שדולת הנשים, ותוצאה שכותרתה "YouTube".
**השורש:** השאילתה הייתה רשימת פסיקים ארוכה — שם העיר היה טוקן בודד
מול ~20 מונחים ארציים כבדים, והמנוע התיישר לפי הרוב.

**אחרי:** אשקלון 88% מזכירות את העיר, 83% עם תאריך, 0 פריטים לא-מסומנים.
- כל 17 השאילתות קוצרו ל-2-4 מילים (רשימה ארוכה **מאפסת** את מצב news)
- מעבר ל-`firecrawlSearchV2({news:true})` — המצב היחיד שמחזיר תאריך
- שם העיר במרכאות; מיון לפי טריות; `normDate` מנרמל ל-ISO
- פריט שלא מזכיר את העיר מסומן `national` ומקבל תג אפור **"רקע ארצי"**
- מסונן google.com/youtube.com — שער, לא מקור
- **`webresearch|v2|` — כל שינוי בשאילתה מחייב העלאת גרסת cache**

**עוד שתוקן:** `/api/alerts` נפל ב-500 (`alert_radar` עם 64 מעקבים חורגת
מ-statement timeout) → נפילה מבוקרת ל-`alert_overview`, העמוד חזר.
המוניטור קיבל **כלל שתי פסילות** — מעריב סיפק 20 פריטים ביום ב-22/23/24
ואפס ב-25 (Vercel נחסם לסירוגין); פיד שנכשל יום אחד אינו פיד מת.

**⚠️ מה עדיין לא מושלם (לדעת לפני שמדגימים):**
- **עיר קטנה/דו-משמעית חלשה יותר** — שלומי ~47% (שלומי הוא גם שם פרטי).
  הפריטים הארציים מסומנים, אז זה כן **מדויק** — פשוט פחות עשיר.
  **להדגים על עיר בינונית-גדולה.** נוסה ונמדד: עיגון שאילתה **אינו**
  הפתרון ("המועצה המקומית" עזר לשלומי, "גליל מערבי" ו"העירייה המועצה"
  החמירו). עוד מונחים = דילול.
- קובייה יכולה לחזור ריקה (אשקלון/תעסוקה=0). ריק אמין יותר מזבל, אבל
  כדאי לבחור קוביות מראש למצגת.
- `alert_radar` נופלת לסירוגין → חצי המגמה 🔥📈📉 נעלמים. תיקון אמיתי
  בצד ה-DB, דורש גישת SQL שאין ל-anon key.
- 21 פידים מקומיים מתים (ingest-only) — לא פוגעים בבית.

## מה פתוח / רעיונות להמשך
- **החזון הגדול:** שמנוע ה-AI ייצר "דוח מודיעין" בסגנון פורטל הגליל **לכל אזור/פרויקט בלחיצה** — הגליל הוא המופע הראשון שמוכיח את הפורמט.
- ~~הבית דליל~~ **נפתר 16.8** (ראה למעלה) — 7→36. אם הוא נראה דליל שוב:
  **קודם כל `/api/feed-health`**, לא הסף ולא המסנן.
- 21 פידים מקומיים מתים לניקוי (ingest-only) — לשאול קודם.
- `/api/narratives` איטי (~37ש' cold, בלי cache).

## מה לעשות עכשיו
תתחיל בלקרוא את `HANDOFF-NEXT-SESSION.md` בריפו + את קבצי הזיכרון הרלוונטיים, ואז תשאל אותי במה ממשיכים. אל תתחיל לבנות בלי שאישרתי.
